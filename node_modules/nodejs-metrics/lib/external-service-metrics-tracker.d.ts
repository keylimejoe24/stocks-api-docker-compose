import { IExternalServiceMetricsTrackingOptions, IExternalServiceMetricsTracker, IMetricsClient } from "./types";
export declare class ExternalServiceMetricsTracker implements IExternalServiceMetricsTracker {
    private metricsTracker;
    constructor({ promClient }: {
        promClient: IMetricsClient;
    });
    trackHistogramDuration<T>({ targetLabel, action, handleResult, }: IExternalServiceMetricsTrackingOptions<T>): Promise<T>;
}
