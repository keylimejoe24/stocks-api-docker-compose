import { RequestHandler } from "express";
import { IMetrics, IMetricsOptions, IPromClient } from "./types";
export declare class Metrics implements IMetrics {
    private logger;
    private backend;
    private expressMiddlewareProvider?;
    private expressMiddlewareSettings?;
    private prometheusMiddleware?;
    constructor({ logger, backend, backendSettings, expressMiddlewareProvider, expressMiddlewareSettings, }?: Partial<IMetricsOptions>);
    init(): Promise<void>;
    getMonitoringMiddleware(): RequestHandler;
    destroy(): Promise<void>;
    getServerPort(): number;
    getClient(): IPromClient;
    private setupExpressMiddleware;
    private getDefaultBackend;
}
