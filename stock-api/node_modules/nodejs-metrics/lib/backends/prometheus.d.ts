import { IBackendSettings, IMetricsBackend, IPromClient } from "../types";
export declare class PrometheusMetricsBackend implements IMetricsBackend {
    private client;
    private logger;
    private metricsServer?;
    private desiredServerPort;
    private serverPort;
    private defaultMetricsTimeout;
    private app?;
    constructor(settings?: Partial<IBackendSettings>);
    getClient(): IPromClient;
    getServerPort(): number;
    startServer(): Promise<void>;
    stopServer(): Promise<void>;
    private setupServer;
    private createServer;
}
